{% extends 'base.html.twig' %}

{% block title %}Barmiton{% endblock %}

{% block body %}


<div class="jumbotron text-center">
    <h1>Toutes les recettes</h1>
</div>

<div class="container">
<a href="{{ path( 'recipeAdd' ) }}" type="button" class="btn btn-success">Ajouter</a>
        {% for recipe in recipes %}

            <h3>
                <a href="{{ path( 'recipeRead', { 'id' : recipe.id } ) }}">{{ recipe.recipeName }}</a>
            </h3>
            <br>

        {% endfor %}

<div/>
{% endblock %}
